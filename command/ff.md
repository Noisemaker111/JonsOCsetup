---
description: Fast forward the current branch to main
---

Fast forward this branch to `main` (or `master`, whichever the repository uses).

Fetch, then fast-forward only. If it is not a fast-forward, stop and say what diverged — do not merge,
rebase or force.
