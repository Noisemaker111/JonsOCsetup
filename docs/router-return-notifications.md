# Return delegated outcomes to the originating conversation

The reported OpenCode2 conversation delivered work to a destination giver and then went idle. The selected OpenCode generation is gen-1788921318839; the earlier Codex hook repair is a different runtime surface. Its DestinationRouter saved a return hub in metadata and prompt text, but no implementation consumed that address to wake the origin. project_result was polling only. Execution subscriptions handled explicit goals, not routed conversation returns.

The saved Quest “Prove Quest delegation with a tiny tested code change” records two failed starts, no worker sessions, and Cannot establish selected Git checkout. The hub is non-Git; config is a junction to the config repository. Both shared and worktree launch paths check the Git source before allocating, so changing workspace mode cannot repair that selection. The Quest remains blocked and unchanged. Automatically moving that record to a different project or weakening project checks is not part of this notification repair.

This candidate builds on the existing runtime-repair source without modifying that checkout. Route admission now saves a durable return watch before destination prompting. A destination terminal event starts a real origin prompt with bounded result excerpts. Worker terminal events resolve the owning giver through the Quest facade, so their evidence follows the same route. A two-minute missing-terminal deadline sends an explicit unconfirmed-outcome prompt. It does not claim a worker started, release ownership or relaunch work.

Return watches and notices live in the router's own storage. Amended requests share one current return address; per-notice claims and persisted states prevent duplicate sends. Unknown prompt admission is preserved rather than retried blindly. project_result includes notification state. accepted means the host accepted the prompt; it does not fabricate an acknowledgment that the assistant understood it. A changed origin agent/model leaves the notice pending instead of substituting a model. Internal notification prompts do not cancel an explicit goal as if they were user steering.

Verification includes mocked durable-state/restart/duplicate/unknown/model-change tests and real OpenCode2 acceptance. The real-host fixture finishes the hub's initial turn, makes a configured-command Quest launch fail in a non-Git destination, receives the destination terminal event, starts an automatic hub turn, and rereads the saved assistant report. No extra user prompt, inference worker or remote model call is used. The deterministic Responses endpoint supplies scripted host responses; success is asserted against the actual saved host message, not a drawn UI or fabricated receipt.

Real-host failed-launch passing receipt: .visual-e2e/router-return-1788973513353/report.json. The real two-minute silent-destination timeout also passed, including saved automatic hub response: .visual-e2e/router-return-1788973634506/report.json. The five notification regressions and existing router tests passed (19 total, 194 assertions). Configuration smoke passed 103 tests, 0 failures. Full suite: 932 passed, 1 existing skip, 0 failed, 7,228 assertions across 136 files (286.32 seconds). Full output is retained in .visual-e2e/full-tests.log.

This is saved candidate source, not an activated repair. The selected OpenCode generation and running sessions have not changed. Existing routed sessions have no newly registered return watch: this does not pretend to retroactively monitor them. After review/merge and an explicitly authorized generation activation, use a fresh verified hub session and rerun the failed-launch and silence acceptance checks before relying on automatic updates. The original smoke-test Quest still needs its editing source bound to config while preserving its saved project identity.

No push, publish, merge, live-generation change, session termination or shared-journal rewrite was performed. Only isolated test subprocesses and fixture records were used. The relocated OpenCode skill was read from .agents/skills/opencode/SKILL.md in the real config checkout; the old skills/opencode/SKILL.md path remains absent there.