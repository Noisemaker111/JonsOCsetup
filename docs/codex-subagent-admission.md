# Native Codex subagent admission

The installed hook treated native collaboration controls as checkout writes and denied Luna dispatch before a child started when another session owned the checkout. Reproduced the exact denial against a protected fixture owner.

Native collaboration controls now bypass parent checkout acquisition. Child shell and patch calls retain their own ownership checks and isolated recovery. Unknown tools remain denied. Candidate and installed-hook checks admitted dispatch, executed an isolated child write, and preserved the owner's source and participant record. A real native Codex gpt-5.6-luna subagent completed after activation.

The original 37-case recovery run had 36 passes and one obsolete assertion requiring native dispatch denial. After correcting that assertion to reject similarly named unknown tools, all four affected control/goal/unknown-tool/child-isolation cases passed. Previous installed hook bytes and hashes are backed up under its repairs directory. Hook definitions and existing journals are unchanged.
