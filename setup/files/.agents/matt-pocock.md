# Skill selection

Most tasks need no skill; load one only when its one-sentence description precisely matches the requested work, and never let it expand authorization or interrupt an already-authorized task.
