# Parked skills

Moved here so they stop loading descriptions into every session.

- 2026-09-05: Matt Pocock pack (never triggered; duplicated grill/router/tracker skills).
- 2026-09-07: global methodology pack, OpenCode extras, Codex Convex docs, Claude `shell-forensics`, OpenCode harness snapshot copies. See `2026-09-07-trim/`.

To re-enable one, move its folder back to `../skills/` (or the host dir it came from). `npx skills@latest update --global` may recreate folders under `../skills/`; park them again afterwards if they are still unused.

Left live globally: `windows-shell`, `github`, `help-i-cant-work-right`. `img2threejs` stays in `~/.claude/skills`. `convex-deploy-guard` stays in `~/.codex/skills`. JGengine / desmakes / vps-code project skills were not touched (live git branches).
