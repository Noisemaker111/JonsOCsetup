# Captured external setup files are exact-byte imports, checked by manifest hash.
/setup/files/** -text
/setup/recovery/live-checkout/files/** -text

# Manifest sources reused from the repository are installed byte-for-byte too, so their checkout
# must not depend on the platform's line-ending conversion.
/skills/** -text
/docs/user-verification.md -text
